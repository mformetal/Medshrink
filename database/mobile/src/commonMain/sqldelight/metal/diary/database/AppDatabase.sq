CREATE TABLE Note(
    id INTEGER NOT NULL PRIMARY KEY,
    title TEXT NOT NULL,
    body TEXT NOT NULL
);

getAllNotes:
SELECT Note.*
FROM Note;

insertNote:
INSERT INTO Note(id, title, body)
VALUES(?, ?, ?);